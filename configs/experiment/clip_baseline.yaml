# EXP-01: Стартовый бейзлайн — EN CLIP, полный fine-tune, lr=5e-6
# Запуск: uv run python scripts/train.py +experiment=clip_baseline
name: clip_baseline
defaults:
  - data: baseline
  - model: clip_vit_b32
  - train: adamw_cosine
  - _self_
