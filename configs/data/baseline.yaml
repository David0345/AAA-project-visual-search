# Исходные данные
data_dir: data/raw/dataset_1M
valid_ids: src/visual_search/data/eda/valid_image_ids.csv
val_csv_raw: src/visual_search/evaluation/val_dataset/val_dataset.csv

# Вывод
output_dir: data/processed
test_frac: 0.3

# Для downstream (train/eval)
images_root: ${data.data_dir}
train_path: ${data.output_dir}/train.parquet
val_path: ${data.output_dir}/val.csv
test_path: ${data.output_dir}/test.csv

image_size: 224
batch_size: 256
num_workers: 8
pin_memory: true
augmentations: light      # light | none | strong
