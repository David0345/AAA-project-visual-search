# Под-конфиг обучения: оптимизатор, scheduler, лосс.
optimizer: adamw
lr: 1.0e-4
weight_decay: 0.2
epochs: 10
scheduler: cosine
warmup_steps: 500
loss: infonce             # infonce (CLIP) | siglip
temperature: 0.07
seed: 42
tracking: csv             # csv | tensorboard | wandb
