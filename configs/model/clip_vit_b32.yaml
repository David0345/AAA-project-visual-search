# English CLIP ViT-B/32 (OpenAI).
# Хорошо работает на визуальных запросах (image mode).
# Для русского текста — хуже xlm_clip_vit_b32, но быстрее обучается.
name: clip_vit_b32
embed_dim: 512
pretrained: openai
freeze_backbone: false
grad_checkpointing: false
