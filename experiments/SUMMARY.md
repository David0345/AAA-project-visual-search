# Итог исследования (bottom line)

> Короткая выжимка. Полный лог траектории — `RESULTS.md`, все замеры — `eval_full_ledger.jsonl`.
> Обновлено: 2026-06-14.

## Задача
Мультимодальный визуальный поиск по одежде Avito (~1M товаров). Три режима: **image** (фото→похожие),
**txt** (русский текст→фото), **multimodal** (фото + текст-модификатор). Цель — поднять mm/all, не уронив txt.

## Как меряем (eval) — БЕЗ ручной разметки
- Протокол: запрос → ранжированная выдача (FAISS) → MRR / Recall@K.
- Метки релевантности **автоматические**: «релевантно = тот же `item_id`» (txt: текст→картинки товара;
  image: картинка→другие фото того же товара).
- Каталог = **52 243 картинки** (val-таргеты + 50k дистракторов), `scripts/eval_full.py`. Старый eval на
  477 картинок завышал метрики ~×3 — пользоваться только eval_full.

## Лучшая модель
**SigLIP 2 ViT-L-16 (256px) + дообучение на синтетических запросах** (lr 1e-6, 2 эпохи).
Чекпойнт: `experiments/l16_synth_lr1e-6/best_ep2_model_only.pt`.

| метрика | значение |
|---|---|
| all-MRR | **0.327** |
| txt | 0.388 |
| image | 0.370 |
| multimodal | 0.223 |

## Прогресс all-MRR (каталог 52k)
| этап | all |
|---|---|
| zero-shot xlm-CLIP (старая база) | 0.151 |
| дообученная xlm-CLIP (наш свип) | 0.188 |
| SigLIP 2 L-16 zero-shot (смена базы) | 0.290 |
| SigLIP 2 gopt-1B zero-shot | 0.304 |
| **L-16 + синтетика (ИТОГ)** | **0.327** (+117% к baseline) |

## Ключевые решения и почему
1. **Сменили базовую модель** на SigLIP 2 (мультиязычная, знает русский) — дало больше, чем весь тюнинг старой
   модели. Marqo-FashionSigLIP (доменная, но английская) проиграла из-за перевода.
2. **Дообучаем — ДА, но только на синтетике.** Шаблонные запросы из метаданных при дообучении ВРЕДЯТ
   (ломают txt: 0.370→0.297). Синтетические запросы Qwen2-VL **по фото** (видят принт/фасон/длину/фактуру —
   сигнал, которого нет в метаданных) — дообучение ПОМОГАЕТ (txt 0.370→0.388, mm +54%).
3. **1 RPS не ограничивает** выбор модели (латентность на запрос мизерная; дорого только офлайн-индексирование).

## Что НЕ сработало (проверено, отклонено)
- Реранкер (Qwen2-VL pointwise поверх top-K) — вредит (bi-encoder уже силён, 0-10 слишком грубо).
- Разрешение 384px — помогает zero-shot, но после синтетики ничья при 2.3× стоимости.
- Hard-negative батчинг (категорийные батчи) — нейтрально.
- Мульти-фото (несколько ракурсов) — не помогает retrieval.
- Заморозка башен, высокий lr — вредят (хрупкий txt).

## Синтетика v2 — 5 стилей запросов (2026-06-15)
Догенерили 60k товаров с 5 стилями (короткий/стандартный/детальный/разговорный/атрибутивный — против
distribution shift по формулировкам). Результат vs v1 (descriptive, 30k):

| модель | all | txt | image | mm |
|---|---|---|---|---|
| **L-16 synth-v1 (descriptive)** | **0.327** | **0.388** | **0.370** | 0.223 |
| L-16 synth-v2 (5 стилей) | 0.319 | 0.356 | 0.355 | **0.247** |

**v2 НЕ обогнал v1 на нашем eval** (txt −0.032, mm +0.024, all −0.008). Причина — наш eval **описательного
стиля**, v1 точно в него попадает; v2 «размазал» по 5 стилям → просел txt на eval, но дал **лучший mm**.
Распознанный distribution shift: разнообразие запросов уводит от стиля eval. **v2 вероятно устойчивее к
реальным запросам, но измерить нечем** (eval одностилевой). **Deliverable остаётся v1 (0.327).**

## Главный вывод
Упёрлись в **потолок измеримости**: дальнейший прогресс требует **другого eval** (разнообразные запросы),
а не ещё синтетики. Лучший измеримый deliverable — **L-16 synth-v1, all 0.327**.

## ★ ФИНАЛ выбора модели (2026-06-16) — Gemini held-out решил спор
Получили независимый diverse-eval: 9.3k запросов Gemini-2.5 (5/товар), 450 товаров отложены в held-out
(НЕ в обучении). Сравнили 3 кандидата на ДВУХ eval (наш descriptive + Gemini held-out), каталог 52k:

| модель | descriptive all | gemini held-out all | сумма |
|---|---|---|---|
| **v1 — descriptive Qwen synth (30k)** ★ | **0.313** | **0.211** | **0.524** |
| v2 — 5 стилей Qwen (60k) | 0.302 | 0.198 | 0.500 |
| mix — Qwen+Gemini (66k) | 0.305 | 0.203 | 0.508 |

**v1 побеждает на ОБОИХ** — даже на Gemini-eval, где у mix было «домашнее преимущество» (учился на Gemini).
Вывод: **простая чистая descriptive-синтетика обобщается лучше шумной/разнообразной.** Разнообразие стилей
и второй LLM-источник не помогли. **ФИНАЛЬНАЯ МОДЕЛЬ = v1.**

## Артефакты (готовы к деплою)
На сервере данных `~/vs_artifacts/` (коммуналка чистилась): `catalog.faiss` (929 MB, 226 921 товар, индекс
на v1), `catalog_meta.parquet` (item_id, image_path), `model.pt` (3.5 GB, v1 ckpt), `interim/` (запросы).
Сборка/инференс: `scripts/build_catalog_index.py`, `scripts/eval_full.py`, `scripts/loadtest.py`.
